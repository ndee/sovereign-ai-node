#!/usr/bin/env node
// CLI entry: emit DEFAULT_*/EXISTING_* TSV from an existing install request
// and runtime config. Pure logic lives in ../load-existing-defaults.mjs.

import { runCli } from "../load-existing-defaults.mjs";

runCli();
